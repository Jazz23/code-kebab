apiVersion: v1
kind: Service
metadata:
  name: {{ include "code-kebab.fullname" . }}
  labels:
    {{- include "code-kebab.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "code-kebab.selectorLabels" . | nindent 4 }}
